# Recipes

Install the dependencies:
```sh
pnpm i
```

Create the tsconfig.json:
```sh
npx tsc --init
```

Start the project:
```sh
pnpm run dev
```

## Prisma
```sh
npx prisma generate
npx prisma run dev
npx prisma studio
```